rules_version = '2';

// Firebase Storage regels voor MatchMe Lover
service firebase.storage {
  match /b/{bucket}/o {
    // Tijdelijk: open regels voor development/debugging
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}